
![[image221.png]]

> ![[image222.png]]

- 小技巧：拖动标签可以将各个视图拖出来形成独立窗口，也可以再拖动标签放回原来位置。
## ⁠1 ⁠压缩图

### ⁠1.1 ⁠压缩图内容简介

![[image223.png]]

- 压缩图：主要的工作区域，包含花样、模块、草稿区等

- 花样图：花样基础视图，由不同的颜色序列形成，与对应的模块结合，生成【展开视图】。

- 前幅组织图：一线成型花样前幅的基础组织色码在此绘制，也可以使用组织模块色码，与花样图和对应的组织模块结合，在生成【展开视图】时应用到对应的前幅花样。

- 后幅组织图：一线成型花样后幅的基础组织色码在此绘制，也可以使用组织模块色码，与花样图和对应的组织模块结合，在生成【展开视图】时应用到对应的后幅花样。

- 系统模块：花样图和组织图对应的系统模块在此排列

通过点击![[image224.png]]（分析工作区域）按钮，可以重新在模块库中检索调用对应模块，并将调用的模块自动按使用顺序排列并编号。

![[image225.png]]

- 用户模块：用户手工修改过的模块可以放到这个区域，从用户模块库调用出来的模块也会放到这个区域。

  - ==“用户模块”优先于“系统模块”，一旦在“用户模块”检索到对应模块，不再去模块库中进行检索。==

- 草稿区：被2针1号色码隔开的右侧区域，用户可以随意绘制，系统不处理此处内容。

<!-- -->

- 此页面点击![[image226.png]]“应用模块并展开”按钮，利用压缩花样、模块、前后幅组织形成“展开视图”

- 注意：

  - 压缩图第一张“花样图”的功能线指令**一般情况下**优先于颜色序列模块中功能线指令

但某些特殊指令，如R3功能线的10、250指令不会被压缩图对应的指令替换。

- 压缩图第二张、第三张的组织图的度目层面代码优先于模块功能线的指令代码

- ==在模块区域外的模块，越靠上方，在刷新时会被排在模块区域内越靠下的地方，会被越优先调用。==

### ⁠1.2 ⁠从展开图提取耙子图层

![[image227.png]]

如果在展开图已经画好了耙子图层，在压缩图的标签点击右键可以“把展开图的耙子图层提取到压缩图”，==注意：执行提取之后“编织参数/耙子图层”设置里“保持之前的拉布花样”会自动变为不勾选。==

==注：由于压缩了耙子图层，所以一些细节会丢失==

- 展开图R2上需要有自动生成的顺序标记：

![[image228.png]]

- 展开视图里绘制了耙子图层：

![[image229.png]]

- 执行“从展开图提取耙子图层”，压缩图得到的如下的耙子图层

![[image230.png]]

### ⁠1.3 ⁠把展开图的内容复制到压缩图

在压缩图的标签右键菜单可以把展开图复制到压缩图，然后可以再次使用组织结构模块生成展开图。

==编织参数设置/耙子图层设置：【保持之前的拉布花样】自动取消勾选。==

==编织参数设置/模块展开：【一线成型拉伸处理】自动取消勾选==

==R19杆上会变成190（不再检索颜色序列模块）==

![[image231.png]]

![[image232.png]]

大体的流程是这样的：

1\. 大家按照之前的习惯画好一个版子，包括画度目图，在展开图修耙子图层。后面发现有问题需要进行改动。

2\. 把版子另存为一个新的，例如XXX－展开图修改。

3\. 在压缩图界面右键点击压缩图标签，选择“把展开图的内容复制到压缩图”。然后软件会问“是否继续，这样会覆盖掉原有的压缩图”，选择是。

4\. 然后软件会把展开图的所有内容（包括度目图、耙子图层）复制到压缩图。

5\. 在压缩图界面，我们可以再次使用组织结构模块。

6\. 然后进行相关设置，主要是耙子图层计算的设置。是否保留之前的耙子图层，这里我们一般要选择不保留之前的图层。设置百分比。

7\. 点击展开按钮，再次展开。这里我们再次展开跟以前的展开不太一样，基本上不需要再次修改耙子图层，因为展开的都是组织结构模块。

==把展开图复制到压缩图之后，为了提高速度，在复制展开图到压缩图的时候：==

> ==①软件自动调整了画版的尺寸，删除上方空白的行。==

==②软件自动删除用户区域的纱嘴循环模块。==

### ⁠1.4 ⁠在压缩图界面展开编织顺序模块

![[image233.png]]

可以展开编织顺序模块，但是不展开组织结构模块。

==编织参数设置/耙子图层设置：【保持之前的拉布花样】自动取消勾选。==

==编织参数设置/模块展开：【一线成型拉伸处理】自动取消勾选==

==R19杆上会变成190（不再检索颜色序列模块）==

![[image231.png]]

## ⁠2 ⁠展开视图

![[image234.png]] ![[image235.png]]

- 展开视图：展示花样细节，也可做一些细节修改。

- 此视图界面点击![[image236.png]]“编译处理”按钮，可以进行编译检查，产生编译结果。

- 右键菜单中点击“跳到对应模块”，可以跳转到压缩图对应模块

- 展开报告：汇总和追踪展开过程中出现的问题

## ⁠3 ⁠简化视图

![[image237.png]]

- 简化视图：将展开视图编译成功后会自动生成，用更基础的色码和功能线表达各个编织动作。

- 有些情况编译的时候为了整耙处理或者避让同导轨纱嘴需要加宽画版宽度。简化视图里加宽的部分在上方用T1=100标记出来。

- 右键菜单中点击“跳到对应模块”，可以跳转到压缩图对应模块

![[image238.png]]

- 简化视图里：==选针==以非参照摇床的针位来表达；==纱嘴区域==以摇床后各选针对应的前床针位表达。

==压缩图：==

![[image239.png]]

==展开视图：==

![[image240.png]]

==简化视图：==

![[image241.png]]

- ==“压缩图”“展开视图”“简化视图”和“模拟线圈图”可以通过双击鼠标左键同步显示位置。==

  1.  简化视图也可以进行细节修改并再次编译。

  2.  注意此时不再分析功能线的层面等指令。

<!-- -->

- 使用\>\>和\<\<来显示“异步踢纱嘴行”的纱嘴运动方向。

> ![[image242.png]]

- 使用=\>和=\>来显示“非异步踢纱嘴行”的机头行进方向。

> ![[image243.png]]

- 也会显示当前花版行系统的使用情况等

- 使用不同的线圈来表示紧编织和紧吊目的线圈。

R13=200 激活紧编织、紧吊目：

对27（前紧编织）、28（后紧编织）、111（前紧吊目）、112（后紧吊目）四个色码有效。

模拟线圈设置里新增“紧密度线圈”颜色的设置。（不显示纱线颜色时）：

![[image244.png]] ![[image245.png]]

- 循环的显示

> ![[image246.png]]

## ⁠4 ⁠“局部展开”视图

![[image247.png]]

局部展开视图：压缩视图上选择一部分，执行局部展开形成。方便快速查看局部展开效果。

![[image248.png]]

## ⁠5 ⁠“文件库中模块”视图

![[image249.png]]

文件库中的模块可以双击打开，进行查看和修改等

## ⁠6 ⁠“源模块”视图

![[image250.png]]

花样视图中，某个模块上按右键，【从模块库中打开源模块】，进行查看和修改等。

![[image251.png]]

## ⁠7 ⁠尺码

显示当前的尺码

## ⁠8 ⁠机型、机器配置

![[image252.png]]

显示当前的机型

- ![[image253.png]]恢复机器默认参数

![[image254.png]]

- 点击机型可以在下拉列表中选择切换成其他机型。

  - 这里展示的机型是在偏好设置里选择的“预设的机型列表”；

  - 如果“预设的机型列表”为空，则显示所有可用的机型列表。

![[image255.png]]

- 点击右侧的“![[image256.png]]”出现机器配置，部分选项可以双击进行修改。

### ⁠8.1 ⁠其他

![[image257.png]]

- 带簧片的织针：是/否

- 用户的机器名(2.07)：2.07版本之前的机器名（注：2.07版本机器名有更改）

- 启用牵拉梳：可选是否启用牵拉梳

> ![[image258.png]]
>
> ![[image259.png]]

- 2段度目系统：可设置哪些系统支持2段度目

> ![[image260.png]]

- 耙子电机驱动：可设置“耙子电机驱动”选项

如果选【是】，编译时符合条件的耙子图层色码会被替换成31号图层代码。

条件如下： 耙子图层是3号或10号代码，并且该行是翻针行，同时整个耙子上没有任何选针。

> ![[image261.png]]

![[image262.png]]

- 配备有耙子：可选是否配备有耙子

  - 是：配合【耙子电机驱动】选项

  - 否：【耙子电机驱动】选项无效

注：选【否】时，编译选项下图选项取消（切换回配备耙子时，此选项会出现并保持之前激活状态）

![[image263.png]]

- 摇床类型

![[image264.png]]

可选：后摇床、双摇床、前摇床

==前后双摇床的机型如860系统机型，请设置成双摇床，纱嘴停放位置等才正确。==

- 纱入纱出系统限定

  - 不限定：所有系统都可以进行纱入纱出

  - 使用1个系统：只使用最外侧1个系统进行纱入纱出

  - 使用2个系统：只使用最外侧2个系统进行纱入纱出

    请根据实际机型限制进行选择

    ![[image265.png]]

- 左侧配备夹子剪刀：是/否

- 右侧配备夹子剪刀：是/否

  - 如果选否，则编译时会忽略R10功能线上的剪刀夹子指令。

- 用户的机器名：机器名

- 罗拉类型：上罗拉、下罗拉、上罗拉和下罗拉、上罗拉（3D罗拉皮+前后罗拉单独驱动）

  ![[image266.png]]

- <span id="压脚类型" class="anchor"></span>压脚类型：

  ![[image267.png]]

  选择不同压脚类型，R11.1功能线显示不同的指令，并且执行相应的编译规则

  如果机器配置没有压脚，则编译时忽略压脚指令并取消错误指令检测。

### ⁠8.2 ⁠常量

- 显示机型基本信息

![[image268.png]]

### ⁠8.3 ⁠纱嘴

- 支持纱嘴弹跳功能的四针板机型可以将纱嘴弹跳功能激活

- 对应纱嘴编织结束停在编织区内时，会复位移动到编织的最后一枚针，并升起。待机器兼容

- ==注：两针板机器暂未配备可弹跳纱嘴==。

![[image269.png]]

![[image270.png]]

- 保存用户机型

![[image271.png]]

可以将修改过参数的机型保存为用户机型，方便以后选用。

> ![[image272.png]]


